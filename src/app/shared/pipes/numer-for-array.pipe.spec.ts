import { NumerForArrayPipe } from './numer-for-array.pipe';

describe('NumerForArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new NumerForArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
